 pipeline {
        agent any
        stages {
              stage("Run Unit test"){
                 steps{
                     sh "./src/manage.py test"
                 }
             }
        }
 }
